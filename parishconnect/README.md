# ParishConnect Frontend (Vite + React)

## Setup
```bash
cd parishconnect
npm install
npm run dev
```
Create a `.env` file (optional) to point to your API:
```
VITE_API_URL=http://localhost:4000/api
```
